import 'package:flutter/material.dart';

class Traffic extends StatelessWidget {
  const Traffic ({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("This is traffic portal in mess!!");
  }
}